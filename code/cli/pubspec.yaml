name: ape_cli
description: >
  CLI for the finite ape machine — workspace initialization and orchestration.
version: 0.0.9

environment:
  sdk: ^3.8.1
dependencies:
  cli_router: ^0.0.2
  modular_cli_sdk: ^0.2.0
  path: ^1.9.1
  yaml: ^3.1.3
dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.0
